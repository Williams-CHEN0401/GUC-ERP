# 維修日誌獨立報價與既有資料整理

日期：2026-09-23。狀態：修正與隔離演練完成；使用者已確認沿用相同人員／權限、取貨跟隨拆分維修，並授權整理正式資料後發布。本文為發布前驗收證據，正式結果另記發布紀錄。

## 來源與範圍

- 本次文件：D:/codex/ERP系統開發/Markdown/ERP-TASK-PROMPT0923.md（本次內容為日誌／報價連動，不沿用舊版本要求）。
- ERP 遠端 main：892da46a743fca9aa17d38a8cc988460a15c54f4；已 fetch。實作分支 codex/repair-quotation-20260923。
- Quotation 遠端 main：5998195587d250bc92d587e72cd260326a341911；已 fetch，與供測試的本機 64ab4d9 內容一致。沒有修改報價網站程式。
- 按 guc-website-maintainer、erp-change-guard 採最小修改及隔離保存驗證；Supabase 查閱當日 changelog 與 database functions 文件。
- 工程施工保留同客戶、同名的既有合併機制。新增維修／查修每次建立獨立工作內容，讓現有報價清單顯示獨立「未報價」列；不憑空建立有金額的報價單。
- 同名維修加入日誌日期 YYMMDD，再碰撞使用 -2、-3；超過 120 字明確拒絕，不截斷。
- 既有日誌編輯／改歸屬／手動改名沿用原機制，不因修改明細再拆分。

## 實作

- app.js：新增維修不被既有同名工作的類型／狀態覆蓋，預覽命名、保存後採伺服器解析名稱核對；仍核對客戶、科室、日誌日期、內容、施工人員與維修明細。
- customer-departments.js：新增獨立維修以目前表單科室驗證，不誤認同名舊工作為保存目標。
- migration：20260923060629_repair_visit_quotation_rows.sql，CLI 產生。既有 department-v1 仍 SECURITY INVOKER，僅新增維修分流到維修保存／私有重送紀錄邊界。新增函式撤銷 PUBLIC／anon／authenticated 執行權，service_role 仍須通過既有 actor RBAC。
- 重送檢查在建立工作前，以原始請求（含科室）比對；同 request_id 重送不重複建立，改 payload 則拒絕。相同客戶命名以 customer row lock 序列化。
- 新維修沿用所選原工作 assigned_to 與 project_workers 的同一組人員和全部權限旗標；不把施工人員自動擴充為授權人員。scoped 帳號必須原已獲准 CREATE，客戶／科室／來源名稱也必須吻合。
- 未增加客戶、設備或身份主檔；未修改 RLS、金額、報價狀態、發票或歷史版本。

## 驗收證據

| 案例 | 結果 |
|---|---|
| npm run check | 294 / 294 通過 |
| verify-repair-visit-db.mjs | 日期尾碼、-2/-3、重送、工程同名合併、編輯不再拆分、無權限、科室錯誤、維修事件失敗整筆回滾、120 字上限及 invoker/ACL 通過 |
| 報價實際 view/RPC | 4 次同名維修顯示 4 列；2 次同名工程施工顯示 1 列 |
| 前端重讀測試 | 拒絕錯誤客戶、科室、名稱、施工人員及工作 ID；兼容尚未升級的後端回傳，未放寬舊版核對 |
| 瀏覽器一次完整流程 | 在 4217 開新增日誌，選隔離客戶／科室／維修類型，輸入「電話查修」、2026-09-15、施工人員、故障內容與處理流程，送出實際 Gateway→RPC→PGlite，成功後重新整理 |
| 瀏覽器重讀 | 舊 9/1「電話查修」保留，新 9/15「電話查修260915」獨立存在；保存 API failures=[] |
| 報價資料重讀頁 | 4218 使用同一隔離 DB、未改寫的報價 view／list RPC，顯示兩筆 unquoted。此頁是資料核對頁，不冒稱正式報價 UI |
| git diff --check | 通過（僅 Windows LF/CRLF 提醒） |
| 同一人員／授權 | 實际既有 RBAC：原已授權的 scoped 建立成功且權限完全一致；未授權、已撤銷建立權限及任意改名均拒絕 |
| verify-repair-backfill-db.mjs | 精確清單、過期快照拒絕、途中失敗整批回滾、日期碰撞尾碼、取貨數量／日誌連結、維修歷史／施工人員／權限、原工作附件留存、報價列數、重送無重複均通過 |

環境：http://127.0.0.1:4217/?page=worklogs；報價資料核對：http://127.0.0.1:4218/ 。只限本機；使用合成資料，服務重啟即清除，不連線 NAS／正式 Supabase。登入以測試 adapter 模擬，未宣稱正式 Auth 端到端測試。未測手機實機。site provisioning／編號以既有精簡 fixture 適配，正式 ensure_project_site_v1 已唯讀核對。瀏覽器 CLI 不在 PATH，改以可用的 CUA 原生瀏覽器介面驗證。

可重跑：

```powershell
$env:PGLITE_MODULE='file:///C:/Users/KHUser/Documents/ChatGPT/ERP系統開發/.tmp/worklog-db-test/node_modules/@electric-sql/pglite/dist/index.js'
$env:QUOTATION_REPO='C:/Users/KHUser/Documents/ChatGPT/ERP系統開發/GUC-Quotation-status-0923'
node scripts/verify-repair-visit-db.mjs
node scripts/verify-repair-backfill-db.mjs
npm run check
```

## 正式資料唯讀盤點

- 現有 57 筆有效維修日誌，連到 51 筆有效工作內容。
- 4 組工作內容共合併 10 筆維修；按日誌日期、建立時間、ID 排序，首筆保留原工作／名稱，後續 6 筆待拆出並加日期尾碼。
- 這 4 組沒有任何報價單（包含封存資料），無需重新分派正式報價。另有 3 張報價各屬單獨維修，不變更。
- 待拆的日誌共連結 2 筆取貨與 2 筆維修事件，無日誌附件。2 組原工作另有 3 位與 2 位 project_workers 關聯；不應遺失原本可見性。
- 已查 FK、主要 trigger、ensure_project_site_v1、site→project 對應與 sync queue；不關閉稽核或同步 trigger。
- 已重新備份 4 工作、10 日誌、8 筆原工作取貨（只移其中 2 筆）、5 筆授權、22 筆施工人員關聯、2 筆事件及相關案場。原工作有 2 個未綁日誌的附件，維持原關聯；不搬 NAS 檔案。私人快照與精確計畫留在工作區 .tmp/repair-visit-0923，不加入 Git。

## 已確認的整理及發布計畫

1. 使用者明確選擇：拆分後沿用原工作同一組人員與完全相同的權限；2 筆取貨跟隨對應維修，保留 work_log_id、品項及數量。
2. 只整理已盤點的 6 筆後續維修；第一筆保留原工作，沒有報價金額複製或虛構報價單。既有 3 張單獨維修報價不修改。工程施工不變。
3. scripts/repair-visit-backfill.mjs 僅產出 SQL，不自動連線。私有快照逐列比對，鎖定來源及關聯後單一 transaction；寫入後檢查原工作、剩餘日誌／取貨、全部維修事件／施工人員、相同授權、報價及庫存不變。新版本／新依賴即整批停止；操作標記重跑不重複。
4. 使用者已要求完成後發布。先 PR／CI／Preview，發布相容前端並確認 READY，再精確套本次 migration、執行備份清單的資料整理，最後正式只讀核對。Gateway、報價前端及其他系統不變。舊開啟頁面需重新整理後使用新規則。

核心程式回復可恢復 department-v1 舊分流及對應前端；已新增業務資料不刪除。資料整理需獨立的逐列回復計畫，不能用程式回退當資料回復。

逐列資料回復：先以 operation 稽核取得原／新 project_id、site_id、title 及取貨原行；取得新快照及確認後續無報價、附件、日誌、指派或權限修改，再另開受控 transaction 回復指定日誌與原取貨歸屬，保留歷史、ID、稽核。新工作／案場不硬刪；如已有後續作業，改採人工確認的前向修正，不以舊快照覆蓋。
