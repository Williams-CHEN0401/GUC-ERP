# 工作日誌附件上傳進度

日期：2026-09-16。來源：本次已重新 fetch GitHub `Williams-CHEN0401/GUC-ERP` main，最新 commit 仍為 `7bdcc170ec9dca6881ebe4ecb78e5e696c9a7249`。沿用 `codex/customer-contract-services-20260916` 工作樹，保留上一項承攬內容管理的未提交變更。

## 調整

- 工作日誌的「操作 → 附件」沿用既有附件上傳入口，新增整體及每個檔案的傳送進度條、百分比、成功／失敗／取消數量。
- 依序呈現檢查、準備、傳送、等待 NAS 寫入與校驗、儲存附件紀錄、完成或失敗。傳送 100% 不代表已完成儲存。
- 大照片沿用分段上傳、兩檔並行、簽章票證、同名檔案詢問、原檔校驗及既有索引 API。重試同一段不重複加總進度，不增加最終寫入的自動重試。
- 錯誤保留每個檔案的結果；已寫入 NAS 但索引失敗仍明確提示，不假裝成功。重試依舊要求只挑選失敗檔案。
- 上傳時鎖定表單輸入，但進度區維持清楚且可捲動；阻止重複送出、關閉上傳視窗，重新整理／離頁使用瀏覽器標準提醒。成功後沿用原本關閉視窗及成功提示。
- 沿用現有 Auth、權限、NAS API 及資料關聯，沒有新增資料庫 migration、依賴或正式設定變更。本次變更未推送、未發布。

## 驗證

- `npm run check`：245 項全數通過；包括原有手機原檔、同名處理、權限及分段測試。
- 新增 4 項單元測試：XHR 進度與 HTTP／逾時／斷線錯誤、並行及分段重試累計、部分失敗及取消、HTML 跳脫及進度畫面重設。
- Chrome 實際表單 → XHR → 現有 NAS API → 記憶體 WebDAV，測試 6 MB HEIC 與 1 MB PDF 同時上傳，使用節流驗證中途進度、等待索引狀態、成功關閉及不重複提交。
- 390px 手機測試一檔成功、一檔索引失敗，確認紅色錯誤、成功／失敗計數、可重新選檔，無頁面水平溢出或 JavaScript 錯誤。
- 已檢視桌機及手機截圖：`tmp/attachment-progress/upload-desktop.png`、`tmp/attachment-progress/upload-mobile-error.png`。
- agent-browser 在本環境不可用，依技能回退使用現有 Playwright／Chrome。未上傳任何正式檔案、未操作正式 NAS 或正式資料庫；NAS API 與索引測試使用隔離替代服務。未進行實體 iPhone 或正式 NAS 網路測試。

## 重要檔案

- `attachment-upload.js`：XHR 傳送進度、共用進度資料及畫面。
- `app.js`：接回既有上傳入口、送出／關閉保護及表單狀態恢復。
- `styles.css`：沿用既有配色與進度區樣式。
- `scripts/attachment-progress.test.mjs`、`scripts/verify-attachment-progress-browser.mjs`：回歸驗證。
- `scripts/attachment-progress-preview-server.mjs`：本機記憶體 NAS 模擬，不得作為正式服務。

## 本機驗收

<http://127.0.0.1:4214/?page=worklogs>，開啟「附件進度隔離測試」日誌的「操作 → 附件」，選擇承攬與檔案後上傳。檔名含「失敗」可測試索引錯誤。檔案僅存於本機測試程序記憶體，程序關閉即失效。原承攬管理測試頁 4213 仍保留，本機 4213 不提供 NAS 上傳。

技術參考：[MDN XMLHttpRequest upload](https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest/upload)。進度來自 upload 事件；等待伺服器回應及索引成功後才標示完成。依 erp-change-guard 保留原 API、身份驗證與權限邊界。
