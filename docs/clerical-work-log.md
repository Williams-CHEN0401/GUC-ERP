# 文書作業工作類型（2026-09-11）

依第 8 點需求，在既有工作類型模型加入 `clerical`／「文書作業」。新增、修改、篩選均沿用原工作日誌流程；工作內容與同一工作內容的其他日誌，依原有規則雙向同步類型與狀態。

`20260911095725_clerical_work_log_type.sql` 擴充現有兩個 CHECK 約束與八個既有函式的類型驗證／映射。沒有重設舊資料或新增 Auth、客戶、設備、RBAC 模型。報價適用類型仍是既有的工程、維修、送貨；文書作業不自動進入報價清單。

本次僅發布安全 Preview；正式資料庫 migration 及正式 Gateway 尚未套用。正式發布時，須與前次工作日期／報價同步 migration 一起依序部署，再更新正式 Gateway 與前端。

驗證：ERP 既有檢查及 177 個測試通過；隔離 PostgreSQL 測試確認舊資料與 RPC 權限保持不變，文書日誌建立、修改、同步及既有送貨流程正常。瀏覽器驗證安全預覽中的新增、篩選與修改。完整驗證記錄位於工作區 `verification/work-content-sync/`。
