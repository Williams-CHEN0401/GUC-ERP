# ERP 表單同步、取貨搜尋與設備測試事件 — 驗收

日期：2026-09-23｜狀態：本機未發布｜分支：codex/form-sync-20260923
來源：GUC-ERP origin/main `0c5c62ccabd15da2defca3c843c11b8936c45b2b`（v1.13.2；本次已 fetch 確認）。
測試：[ERP 取貨](http://127.0.0.1:4214/?page=transactions)、[工作日誌](http://127.0.0.1:4214/?page=worklogs)。

## 當日需求演進與範圍

保留 ERP-TASK-PROMPT0923.md 第一階段的搜尋順序、品項排序、多人參考資料更新成果。依使用者後續指示，**設備測試改放維修明細的事件類型，不再新增工作類型**；取貨品項搜尋改為明細下方獨立區塊。報價修正另在 GUC-Quotation-status-0923，有獨立驗收文件。

- 共用客戶搜尋位於分類／客戶／科室上方；進貨訂貨客戶同步。
- 「維修/查修」→「維修明細」→「事件類型」新增設備測試（EQUIPMENT_TEST）；工作類型仍為六種。保留五種既有事件及歷史舊分類規則。
- 多筆取貨及工作日誌登錄取貨：明細下方新增「搜尋品項」。跨有效種類，以名稱、品牌、型號、編號搜尋，按中文名稱排序。結果最多顯示 50 筆並提示縮小關鍵字。
- 點結果帶入第一筆空白明細的種類／品項並保留數量；沒有空白列則新增一列；不覆蓋既有品項，重複品項聚焦原數量，保留批次筆數上限。Enter 不送出表單，中文組字不攔截。
- 單筆編輯及進貨沿用第一階段的搜尋加原生下拉，未額外重排。
- 參考資料同步：開啟、返回視窗、聚焦時更新；可見頁面 15 秒輪詢、焦點最短間隔 3 秒。沿既有 API/RBAC/scope；options_only 排除歷史交易資料。
- 表單文字、日期、數量、勾選、選取 ID 保留；忙碌、隱藏、登入及系統選擇頁不輪詢；忽略登出／換頁／保存後過期回應；保存使用原版本避免背景更新掩蓋衝突。
- 未改 Auth、權限模型、客戶主檔、編號、庫存計算、日誌／取貨關聯、NAS、Site 程式。

## 設備測試的儲存相容性

新 migration：`20260923050521_equipment_testing_maintenance_event.sql`，由 Supabase CLI migration new 產生。
只擴充 maintenance_events_type_check 與既有 upsert_customer_project_work_log_with_maintenance_v1 的事件允許清單，保留函式簽章、ACL、security definer、search_path、稽核、關聯及交易邊界。正式函式內容已唯讀核對，與來源最新定義一致。沒有批次更新舊資料。

設備測試可記錄故障內容、處理流程、日期及設備關聯，但不自動登錄維修品；原 REPAIR/REPLACEMENT 維修品規則不變。

第一階段未發布的 `20260923041505_equipment_testing_work_type.sql` 已移除並由事件 migration 取代；它從未套用正式 DB，沒有刪除業務資料，也無需轉換正式既有工作類型。原實作可由本次修改紀錄追溯；不得部署舊 migration。

## 驗收證據

| 案例 | 實際結果 | 證據／界線 |
|---|---|---|
| ERP 完整檢查 | 289/289 通過 | npm run check |
| 搜尋結果填入 → 保存 → 重讀 | 查修測試電腦 4 台、光纖測試器 1 台，正確客戶／科室／工作內容 | 內建瀏覽器 → 本機 Gateway → PGlite 真實業務 RPC |
| 重複、滿列、上限、停用、未知 ID | 重複不新增；滿列新增；上限拒絕；停用/未知不填入；數量保留 | pickup-item-search.test.mjs；瀏覽器重複及第二筆新增 |
| 搜尋名稱／品牌／型號／編號及排序 | 跨有效種類；空字串不列出全部；中文名稱順序 | 單元測試與瀏覽器 BASE、光纖、測試 |
| Enter 與手機排版 | Enter 不提交；390px 下表單 329/329、搜尋區 296/296 client/scroll 寬度，無橫向溢出 | Chromium viewport／截圖，非 iPhone Safari 實機 |
| 維修事件保存與重讀 | 設備測試事件驗收0923，工作類型維修/查修，事件設備測試，處理流程正確 | 瀏覽器保存後重新開啟日誌 |
| 新事件 SQL | 基準版拒絕；migration 後新增/編輯/設備關聯可重讀；不新增維修品；舊五事件與六工作類型通過 | verify-equipment-testing-db.mjs |
| 原子性／權限／版本 | 新建舊分類、錯誤工作類型、viewer、過期版本拒絕；migration 可重放，簽章/ACL 不變 | 同上 |
| A 新種類/品項 → B 選單 → 取貨保存 | 使用真實 SQL 寫入/重讀，actor、數量、工作關聯正確；新事件經 Gateway 保存；越權／過期版本拒絕 | verify-form-sync-gateway.mjs |
| 第一階段跨使用者 UI 同步 | A 新增種類及品項，B 不重整取得選項；客戶、科室、數量保留；進貨勾選與備註保留 | 第一階段兩個合成 session UI 驗證；本次新搜尋開啟時亦收到後續新增品項 |
| 前次獨立修改回歸 | 日誌/取貨獨立修改、軟刪除留存、每日類型保留 | verify-independent-work-db、verify-independent-work-gateway、verify-daily-work-type-db |
| 前端錯誤 | 本次相關流程 error/warn 無新增紀錄 | 瀏覽器 dev logs |

第一階段「設備測試為第七工作類型」測試已被本次事件測試取代，不再作為現版驗收或發布依據。

## 資料界線與發布

- 本機 4214 是記憶體合成資料，無正式憑證；重啟後重建。UI 的正式模式只為測試實際 API 路徑，明確另有隔離提示。
- PGlite 執行真實業務 RPC，Auth 身份、編號產生器及部分最小 schema/snapshot 為測試 adapter，不等同正式 CRUD 驗收。
- 尚未 commit/push/PR、Vercel/Gateway 發布或套用正式 DB migration。
- 取得當次發布授權後：新事件 migration → inventory-gateway（及既有唯讀 preview handler）→ ERP 前端。報價前端另行發布，無報價 DB/Gateway 變更。
- 無需新增環境變數。正式資料與 iPhone 實機未測；NAS、Site 新類型介面與跨站流程未驗收、不宣稱已更新。
- 遵循 guc-website-maintainer／erp-change-guard：小範圍修改、保留舊資料與權限、隔離保存及重讀後交付。
